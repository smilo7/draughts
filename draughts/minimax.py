from copy import deepcopy



def minimax(board, current_depth, max_depth, is_max):

    if current_depth == max_depth:
        return board

    #red is the maximiser
    if is_max:
        best_value = float('inf')
        #for each move of max player
        #loop through the valid moves for each peice of max_player

    #mimiser (black)
    else:
        best_value = float('-inf')


def max():
    pass

def min():
    pass


def get_moves
